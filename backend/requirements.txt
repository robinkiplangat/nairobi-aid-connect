# FastAPI & Uvicorn
fastapi
uvicorn[standard]

# Pydantic for data validation and settings management
pydantic
pydantic-settings

# MongoDB driver
pymongo
motor

# Redis driver (for message bus)
redis

# Google Agent Development Kit (ADK) - Placeholder, will need to find the actual package name if available
# google-adk

# HTTP client for making requests to external APIs
httpx

# Python-dotenv for environment variable management
python-dotenv

# For Twitter API (if direct integration is chosen over ADK's potential built-in)
tweepy

# For NLP (e.g., Google Cloud Natural Language)
# google-cloud-language

# For Geocoding (e.g., Google Maps Geocoding)
# googlemaps

# Websockets with FastAPI
websockets

# Security & Monitoring
slowapi  # Rate limiting
sentry-sdk[fastapi]  # Error tracking
cryptography  # Data encryption

# Testing
pytest
pytest-asyncio # For testing async code
requests # For testing API endpoints (alternative to httpx for sync tests)

# Google Cloud libraries
google-cloud-aiplatform
google-cloud-secret-manager
google-cloud-logging
google-cloud-monitoring

# Production dependencies
gunicorn
